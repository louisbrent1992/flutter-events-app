## EventEase Server env (production)
# Copy to `server/.env` for local runs, or set these in Cloud Run.

# --- Runtime ---
NODE_ENV=production
PORT=8080

# --- Firebase Admin (required) ---
# JSON string or base64-encoded JSON for your Firebase service account.
FIREBASE_SERVICE_ACCOUNT=

# Only required if you use Firebase Realtime Database (RTDB).
# FIREBASE_DATABASE_URL=https://<project-id>-default-rtdb.firebaseio.com

# --- OpenAI / LLM (required for AI features) ---
OPENAI_API_KEY=
# Optional OpenAI-compatible base URL.
# OPENAI_BASE_URL=https://api.openai.com/v1

# Optional model overrides.
# OPENAI_EVENTS_MODEL=gpt-4o-mini
# OPENAI_EVENTS_VISION_MODEL=gpt-4o-mini
# OPENAI_EVENTS_PLANNER_MODEL=gpt-4o-mini

# Optional OpenAI-compatible alternative (used only if OPENAI_* are unset).
# LlamaAI_API_KEY=
# LlamaAI_API_URL=

# --- Web import extraction fallback (Jina Reader; always-on when needed) ---
# Used automatically when direct fetch/extraction fails or looks too short.
# JINA_AI_BASE_URL=https://r.jina.ai/
# JINA_AI_API_KEY=
# JINA_AI_TIMEOUT_MS=20000
# JINA_AI_RETRIES=1

# --- Email (used for data deletion / admin notifications) ---
# SMTP host/port have defaults; credentials are required to actually send mail.
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Public URLs used in emails (optional; defaults exist in code).
# SERVER_URL=https://eventease.app
# ADMIN_EMAIL=privacy@eventease.app
# ADMIN_DASHBOARD_URL=https://admin.eventease.app

# --- Third-party APIs (optional) ---
# RapidAPI key used by Instagram/TikTok helpers (only if those endpoints are used).
# RAPID_API_KEY=
# YouTube Data API key (only if YouTube import is used).
# YOUTUBE_API_KEY=

